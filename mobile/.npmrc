# expo-router pulls react-dom (for its web support) which wants a newer react
# than the one Expo pins for React Native. Without this, install resolution
# differs between here and EAS — and `npm ci` on the builder rejects the
# lockfile that a local --legacy-peer-deps install produced.
legacy-peer-deps=true
