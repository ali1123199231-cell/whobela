node_modules/
.expo/
dist/
web-build/
*.log

# Generated by `expo prebuild` from app.json — app.json is the source of truth,
# and a committed copy of these drifts from it silently.
/android
/ios

# Firebase's Android config. Contains this project's FCM sender id and API key,
# which are not secrets in the password sense but are still project identifiers
# that do not belong in a public repo.
google-services.json

# EAS local credentials.
credentials.json
